plugins {
    id("com.gradle.enterprise").version("3.1")
}

rootProject.name = "05-build-comparison"

gradleEnterprise {
    buildScan {
        server "https://enterprise-training.gradle.com"
        publishAlways()
        captureTaskInputFiles = true
        tag("lab05")
    }
}
